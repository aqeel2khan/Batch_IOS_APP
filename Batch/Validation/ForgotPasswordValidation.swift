//
//  ForgotPasswordValidation.swift
//  Batch
//
//  Created by CTS-Jay Gupta on 13/12/23.
//

import Foundation

struct ForgotPasswordValidation {
    /*
    func validate(request: ForgotPasswordRequest) -> ValidationResult{
        
        let emptyResult = checkForEmpty(request: request)
        if emptyResult.isEmpty{
            return ValidationResult(success: false, error: emptyResult.error)
        }
        
        return ValidationResult(success: true, error: nil)
    }
    
    private func checkForEmpty(request: ForgotPasswordRequest) -> (isEmpty: Bool, error: RegistrationError?){
        
        if request.email!.isEmpty{
            return (true, .emptyEmail)
        }
        
        return (false, nil)
    }
    
   */
    
}
